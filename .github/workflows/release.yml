name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer

      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Build universal binary
        run: |
          swift build -c release --arch arm64 --arch x86_64
          mkdir -p dist
          cp .build/apple/Products/Release/pxlsmash dist/

      - name: Verify binary
        run: |
          file dist/pxlsmash
          dist/pxlsmash --version
          lipo -info dist/pxlsmash

      - name: Code sign
        if: env.SIGNING_IDENTITY != ''
        env:
          SIGNING_IDENTITY: ${{ secrets.SIGNING_IDENTITY }}
        run: codesign --sign "$SIGNING_IDENTITY" --options runtime dist/pxlsmash

      - name: Create archive
        run: |
          cd dist
          tar -czf pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.tar.gz pxlsmash
          zip -j pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.zip pxlsmash
          shasum -a 256 pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.tar.gz > pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.tar.gz.sha256
          shasum -a 256 pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.zip > pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.zip.sha256

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.tar.gz
            dist/pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.tar.gz.sha256
            dist/pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.zip
            dist/pxlsmash-${{ steps.version.outputs.VERSION }}-macos-universal.zip.sha256
